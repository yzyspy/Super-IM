type UserInfoRequest {
	UserID uint `header:"uid"`
	Role   int8 `header:"role"`
}

type UserInfoResponse {
	UserID   uint   `json:"uid"`
	Nickname string `json:"nickname"`
	Avatar   string `json:"avatar"`
	Role     int8   `json:"role"`
}

service users {
	// 查询当前登录用户信息
	@handler UserInfo
	get /api/user/user_info (UserInfoRequest) returns (UserInfoResponse)

    // 更新当前登录用户信息
	@handler UpdateUserInfo
	post /api/user/update_user_info (UserInfoRequest) returns (UserInfoResponse)

    // 查询用户某一个好友的信息

    // 查询用户好友列表
}

//goctl api go -api user_api.api -dir . --home  ../../my_template/
