type Response {
	Code int    `json:"code"`
	Msg  string `json:"msg"`
	Data string `json:"data"`
}

type ImageRequest {}

type ImageResponsse {
	Url string `json:"url"` //图片地址
}

type ImageShowRequest {}

type ImageShowResponse {}

service file {
	@handler Image
	post /api/file/image (ImageRequest) returns (ImageResponsse) //图片上传

	@handler ImageShow
	get /api/file/uploads/:imageType/:imageName (ImageShowRequest)
}

//goctl api go -api file_api.api -dir . --home  ../../my_template/
